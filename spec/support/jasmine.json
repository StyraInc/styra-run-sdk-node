{
  "spec_dir": "spec", // should rename this folder tests instead of spec?

  // why not call all tests "*.test.js"?
  "spec_files": [
    "**/*[sS]pec.?(m)js"
  ],
  "helpers": [
    "helpers/**/*.?(m)js"
  ],
  "env": {
    "stopSpecOnExpectationFailure": false,
    "random": true
  }
}
